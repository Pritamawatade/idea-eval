1. Create a post request to /api/evalute with the following request body
 * Request body:
 * {
 *   ideaName: string (required)
 *   description: string (required)
 *   targetMarket?: string
 *   industry?: string
 *   monetizationModel?: string
 * }
2. Add the data of any startup idea into the place holders of request body. make sure you don't leave it black or add non meaningfull text